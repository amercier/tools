{
  "private": true,
  "dependencies": {
    "eslint-config-amercier": "^0.3.0",
    "react": "^16.8.1",
    "react-dom": "^16.8.1"
  },
  "devDependencies": {
    "eslint": "^5.13.0",
    "eslint-plugin-react-hooks": "^1.0.1",
    "husky": "^1.3.1",
    "if-ci": "^3.0.0",
    "lint-staged": "^8.1.3",
    "next": "^8.0.0",
    "npm-run-all": "^4.1.5",
    "prettier": "^1.16.4"
  },
  "scripts": {
    "dev": "next",
    "build": "next build && next export -o dist",
    "eslint": "eslint --ext js,jsx --ignore-path .gitignore .",
    "prettier": "prettier --ignore-path .gitignore '**/*.{html,json,md,s?css,ya?ml}'",
    "lint": "run-p lint:*",
    "lint:prettier": "if-ci run-p 'prettier --list-different' && if-not-ci run-p 'prettier --write'",
    "lint:eslint": "if-ci run-p eslint && if-not-ci run-p 'eslint --fix'"
  },
  "lint-staged": {
    "*.{html,json,md,s?css,ya?ml}": [
      "prettier --list-different"
    ],
    "*.{js,jsx}": [
      "eslint",
      "jest --bail --findRelatedTests"
    ]
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "prettier": {
    "singleQuote": true,
    "trailingComma": "all"
  },
  "eslintConfig": {
    "plugins": [
      "react-hooks"
    ],
    "extends": [
      "airbnb",
      "amercier/lib/more",
      "plugin:unicorn/recommended",
      "jsdoc-strict",
      "plugin:prettier/recommended",
      "prettier/react"
    ],
    "rules": {
      "react-hooks/rules-of-hooks": "error",
      "unicorn/filename-case": [
        "error",
        {
          "case": "kebabCase"
        }
      ]
    },
    "overrides": [
      {
        "files": [
          "components/**/!(index).jsx"
        ],
        "rules": {
          "unicorn/filename-case": [
            "error",
            {
              "case": "pascalCase"
            }
          ]
        }
      },
      {
        "files": [
          "components/**/*.{spec,test}.jsx"
        ],
        "rules": {
          "unicorn/filename-case": "off"
        }
      },
      {
        "files": [
          "**/*.js"
        ],
        "rules": {
          "import/prefer-default-export": "off",
          "import/no-default-export": "error"
        }
      }
    ]
  }
}
