language: node_js
node_js: node # Current
cache: yarn
before_deploy: yarn build
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  local_dir: build
  on:
    branch: master
